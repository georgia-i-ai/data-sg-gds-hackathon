# URL of your running liteLLM proxy
LITELLM_PROXY_URL=http://localhost:4000

# Model name as configured in your liteLLM proxy
LITELLM_MODEL=gpt-4o

# API key for the liteLLM proxy (can be anything if no auth configured)
LITELLM_API_KEY=anything
